//explain math.ceil , math.sqrt() , math.min() , math.max(),
// math.pow() with suitable example

/*
import math
1. math.ceil()-> this function rounds up the value to the nearest right side

2. math.sqrt() = > This function gives square root of the given argument.
3. math.min() -> This function return minimum value from the given argument.
4. math.max() -> this function return maximum value from the given argument

5. math.pow() => this function multiple first argument by the second argument
 */
fun main(){

println(Math.ceil(3.1))
    println(Math.floor(3.1))
println(Math.sqrt(64.0))
println(Math.min(10.0,20.0))
println(Math.max(10,2))
println(Math.pow(2.0,3.0))

}
